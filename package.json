{
  "name": "ponto-digital-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend modular por feature para aplicacao de ponto digital.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch api/src/server.js",
    "start": "node api/src/server.js",
    "test": "node --test tests/*.test.js",
    "lint": "find api tests -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
